== Terrain concept ==

Structure:

irenderable - interfejs dla każdego obiektu który można wyrenderować. Przekazywane do renderera jako vector



terrain/
	terrain .h/.cpp
	terrainLayer .h
	terrainGenerator .h/.cpp
	
terrain.h - przechowuje referencje do mesh, material, i tekstur (heightmap, normalmap, splatmap)
terrainGenerator - używa compute shaderów do generacji tekstur (heightmap, normalmap, splatmap)


terrain.h

public:
	Terrain(sptr<Mesh>, sprt<Material>);
	
	void Draw(Renderer&) - ustawia mesh, material i renderuje
	
	Gettery

private:

	sptr<Mesh>
	sptr<Material>
	sptr<Textures>
	uptr<NoiseGenerator>
	int size;
	
	struct NoiseSettings - podstawowe parametry szumu
	
	
terrainGenerator.h

public:
	TerrainGenerator()
	
	TerrainTextures GenerateTerrain(NoiseSettings&, size);
	
private:
	GenerateHeightMap()
	GenerateNormalMap()
	GenerateSplatMap()
	
	struct TerrainTextures
	{
		sptr<HeightMap>
		sptr<NormalMap>
		sptr<SplatMap>
	}
	
CLEAN MESH!
return guard in material.h!


NoiseControls(NoiseSettings&)


TODO

1. Generator DONE
2. Terrain DONE
3. ComputeShader
4. Renderable DONE
5. Renderer DONE
6. terrainMaterial
7, terrainShader
8. Editor scene

	